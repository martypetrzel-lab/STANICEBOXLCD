const q=(s)=>document.querySelector(s);
q(".menu")?.addEventListener("click",()=>q(".top nav")?.classList.toggle("open"));
document.querySelectorAll("[data-confirm]").forEach(f=>f.addEventListener("submit",e=>{if(!confirm(f.dataset.confirm))e.preventDefault()}));
q("[data-danger-command]")?.addEventListener("click",e=>{if(q('[name="type"]')?.value==="RESTART_DEVICE"&&!confirm("Opravdu chcete zařadit restart zařízení?"))e.preventDefault()});
function tick(){const now=new Date(),clock=q("#clock"),date=q("#date");if(clock)clock.textContent=now.toLocaleTimeString("cs-CZ",{timeZone:"Europe/Prague"});if(date)date.textContent=now.toLocaleDateString("cs-CZ",{weekday:"long",day:"numeric",month:"long",year:"numeric",timeZone:"Europe/Prague"});
const cd=q("#countdown");if(cd){const s=Math.max(0,Math.floor((new Date(cd.dataset.end)-now)/1000));cd.textContent=`${String(Math.floor(s/3600)).padStart(2,"0")}:${String(Math.floor(s%3600/60)).padStart(2,"0")}:${String(s%60).padStart(2,"0")}`}}tick();setInterval(tick,1000);
const form=q("#message-form"),lcd=q("#lcd");function preview(){if(!form||!lcd)return;const raw=`${form.title.value}\n${form.body.value}`.normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/\s+/g," ").trim(),words=raw.split(" ");let lines=[],line="";for(let w of words){if((line+" "+w).trim().length<=20)line=(line+" "+w).trim();else{if(line)lines.push(line);while(w.length>20){lines.push(w.slice(0,20));w=w.slice(20)}line=w}}if(line)lines.push(line);[...lcd.children].forEach((el,i)=>el.textContent=(lines[i]||"").padEnd(20,"·"))}form?.addEventListener("input",preview);preview();
setInterval(()=>{if(location.pathname==="/")fetch("/api/admin/dashboard").then(r=>r.json()).then(d=>{const b=q("#online");if(b){b.className=`badge ${d.online?"ok":"bad"}`;b.innerHTML=`<i></i>${d.online?"Online":"Offline"}`}}).catch(()=>{})},12000);
